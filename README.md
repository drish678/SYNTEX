
  # SYNTEX App Interface Design

  This is a code bundle for SYNTEX App Interface Design. The original project is available at https://www.figma.com/design/awriPTP76k45QMLb5TK724/SYNTEX-App-Interface-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  